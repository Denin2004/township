{
    "widgets": {
    },
    "accountManager": {
        "count": "{{path('accMngrCount')}}",
        "page": "{{ path('accMngrPage') }}",
        "accounts": "{{ path('accMngrAccounts') }}",
        "accountForm": "{{ path('accMngrAccountForm') }}",
        "accountPost": "{{ path('accMngrAccountPost') }}",
        "passwordForm": "{{ path('accMngrPasswordForm') }}",
        "setPassword": "{{ path('accMngrSetPassword') }}",
        "passwordChangeForm": "{{ path('accMngrPasswordChangeForm') }}",
        "changePassword": "{{ path('accMngrChangePassword') }}"        
    },
    "electricity": {
        "uploadForm": "{{ path('electricityUploadForm') }}",
        "upload": "{{ path('electricityUpload') }}"
    }
}
