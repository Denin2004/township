{
    "widgets": {
        "machines": "{{ path('machinesWidget') }}",
        "players": "{{ path('playersWidget') }}",
        "locationNetWin": "{{ path('locationNetWinWidget') }}",
        "cageCurrentBalance": "{{ path('cageCurentBalanceWidget') }}",
        "topPayouts24": "{{ path('machinesWidgetPayouts') }}",
        "favoriteGames": "{{ path('gamesWidgetFavorite') }}",
        "totalBetGames": "{{ path('gamesWidgetTotalBet') }}",
        "locationOccupancy": "{{ path('locationWidgetOccupancy') }}"
    },
    "pages": {
        "systemAlerts": "{{ path('sysMonAlerts') }}"
    },
    "systemMonitor": {
        "page": "{{ path('sysMonShowPage') }}",
        "unread": "{{ path('sysMonUnread') }}"
    }
}
